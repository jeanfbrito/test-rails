<h1>Todos os Estudantes</h1>

<table>
  <tr>
    <th>Nome</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @students.each do |student| %>
  <tr>
    <td><%= student.name %></td>
    <td><%= link_to 'Exibir', student %></td>
    <td><%= link_to 'Editar', edit_student_path(student) %></td>
    <td><%= link_to 'Deletar', student, method: :delete, data: { confirm: 'Você tem certeza que deseja excluir este aluno?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Novo Estudante', new_student_path %>
